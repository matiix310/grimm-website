# authentik base URL (no trailing slash)
AUTHENTIK_URL=https://authentik.example.com

# authentik API token (needs users + groups scopes)
AUTHENTIK_TOKEN=ak_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx